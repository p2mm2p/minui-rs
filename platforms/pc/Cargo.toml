[package]
name = "platform-pc"
version = "0.1.0"
edition = "2021"
description = "MinUI PC desktop platform — reference implementation using minifb"

[dependencies]
platform = { path = "../../crates/platform" }
common = { path = "../../crates/common" }
minifb = "0.27"
log = "0.4"

[lib]
name = "platform_pc"
